/**
 * Applying the plugin here seems to make it available
 * for this project and this project only.  Applied
 * scripts do not have access to the plugin.
 */
//plugins {
//    id "de.undercouch.download" version "3.2.0"
//}
//import de.undercouch.gradle.tasks.download.Download

apply from: 'fragment.gradle'

/**
 * This only works if the plugin is applied here in the
 * child plugin.  It does NOT work if the plugin is
 * applied at the root level, and you cannot apply the
 * plugin in both places.  Pick one.
 */
//task childDownload(type: Download) {
//    src 'http://www.google.com'
//    dest 'build/google.txt'
//}