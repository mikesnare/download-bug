/**
 * This never works.  And since plugins cannot be applied
 * in this file, there's no way to make it work.
 */
task fragmentDownload(type: Download) {
    src 'http://www.google.com'
    dest 'build/google.txt'
}